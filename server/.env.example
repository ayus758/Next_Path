# MongoDB Atlas connection string for local development.
# Replace with your own URI before running the server.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<database>?appName=<app-name>

# Secret for JWT signing. Use a long random string.
JWT_SECRET=replace-with-a-long-random-string
